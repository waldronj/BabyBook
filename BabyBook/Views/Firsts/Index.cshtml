@model IEnumerable<BabyBook.Models.First>

@{
    ViewBag.Title = "Index";
}

@section featured
{
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>First Events</h1>
                <h2>anything new that our son has done.</h2>
            </hgroup>
        </div>
    </section>

}

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FirstEvent)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.FirstValue)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FirstEvent)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.FirstValue)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
