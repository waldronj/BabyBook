@model BabyBook.Models.First

@{
    ViewBag.Title = "Create";
}

@section featured
{
    <section class="featured">
        <div class="content-wrapper">
            <hgroup class="title">
                <h1>First Events</h1>
                <h2>anything new that our son has done.</h2>
            </hgroup>
        </div>
    </section>

}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>First</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.FirstEvent)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FirstEvent)
            @Html.ValidationMessageFor(model => model.FirstEvent)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.FirstValue)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FirstValue)
            @Html.ValidationMessageFor(model => model.FirstValue)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
